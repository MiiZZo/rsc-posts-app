import { Button, Container, Input } from '@mantine/core';

export default function Index() {
  return (
    <Container>
      <Button>Click</Button>
      <Input placeholder="Email" />
    </Container>
  );
}
